# Legacy: self-hosted FastAPI monolith

This is the first version of the platform (FastAPI + SQLite, single process). It was superseded by the
Supabase architecture in the repository root (`supabase/`, `web/`, `worker/`). Kept for reference; the
runner/scoring code it used now lives in `worker/` and `scoring/`.
